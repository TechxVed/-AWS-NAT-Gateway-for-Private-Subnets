# -AWS-NAT-Gateway-for-Private-Subnets
This project demonstrates how to configure an AWS NAT Gateway to provide secure outbound internet access for instances running in a private subnet, while keeping them inaccessible from the public internet.  It builds on a custom VPC architecture consisting of public and private subnets, an Internet Gateway, Route Tables, and Amazon EC2 instances.
